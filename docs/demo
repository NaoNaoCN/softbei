● 项目分析总结

  项目概述

  这是一个第十五届中国软件杯大学生软件设计大赛 A3
  赛题的参赛项目，是一个基于多智能体（Multi-Agent）架构的个性化学习资源生成与学习系统。

  技术架构

  ┌────────────┬────────────────────────────────────────────┐
  │    层级    │                   技术栈                   │
  ├────────────┼────────────────────────────────────────────┤
  │ 前端       │ Streamlit + ECharts                        │
  ├────────────┼────────────────────────────────────────────┤
  │ 后端       │ FastAPI + Uvicorn (异步)                   │
  ├────────────┼────────────────────────────────────────────┤
  │ Agent 框架 │ LangGraph 0.1+ / LangChain 0.2+            │
  ├────────────┼────────────────────────────────────────────┤
  │ LLM        │ 讯飞星火大模型 (主) / OpenAI 兼容接口 (备) │
  ├────────────┼────────────────────────────────────────────┤
  │ 向量库     │ ChromaDB (本地持久化)                      │
  ├────────────┼────────────────────────────────────────────┤
  │ 关系数据库 │ PostgreSQL (生产) / SQLite (开发)          │
  ├────────────┼────────────────────────────────────────────┤
  │ ORM        │ SQLAlchemy 2.x (异步)                      │
  └────────────┴────────────────────────────────────────────┘

  核心功能模块

  1. 10 个协作 Agent (backend/agents/):
    - ProfileAgent - 从对话提取/更新学生画像
    - PlannerAgent - 分析意图，决定资源类型
    - DocAgent / MindmapAgent / QuizAgent / CodeAgent / SummaryAgent - 生成5种学习资源
    - SafetyAgent - 幻觉检测与来源核查
    - RecommendAgent - 推荐下一步学习
  2. RAG 管道 (backend/rag/): 文档解析 → 切分 → 嵌入 → 向量检索 → 上下文格式化
  3. 数据库设计: 13张表涵盖用户、画像、学习路径、资源、测验、知识图谱等

  项目状态评估

  已完成部分:
  - ✅ 完整的项目结构搭建
  - ✅ LangGraph 状态机与 Agent 拓扑
  - ✅ FastAPI 路由框架
  - ✅ Pydantic v2 数据模型
  - ✅ ChromaDB 封装
  - ✅ Streamlit 前端页面框架

  待实现部分 (大量 TODO):
  - ❌ 用户认证 (JWT) 未实现
  - ❌ 数据库 CRUD 操作大部分是 stub
  - ❌ LLM 嵌入功能未实现
  - ❌ 知识图谱 API 未实现
  - ❌ 资源生成任务未接入后台任务
  - ❌ 测验系统未实现

  关键文件

  - backend/main.py - FastAPI 入口，299行
  - backend/agents/graph.py - LangGraph 状态机定义
  - backend/models/schemas.py - Pydantic 模型定义
  - backend/services/llm.py - LLM 调用封装
  - backend/db/vector.py - ChromaDB 封装

  这是一个骨架项目，核心架构已搭好但业务逻辑大部分还是 TODO 状态，适合作为参赛起点。