# 项目实现状态总览

**最后更新**: 2026-04-26

**总体进度**: 103/104 (99%) — 端到端对话式资源生成流程已跑通

---

## **📊 统计概览**

| **模块** | **已实现** | **部分实现** | **未实现** | **完成率** |
| --- | --- | --- | --- | --- |
| FastAPI 路由 | 29 | 0 | 0 | 100% |
| Agent 节点 | 16 | 0 | 0 | 100% |
| Profile 服务 | 5 | 0 | 0 | 100% |
| Resource 服务 | 8 | 0 | 0 | 100% |
| Pathway 服务 | 10 | 0 | 0 | 100% |
| Generation 服务 | 3 | 0 | 0 | 100% |
| LLM 服务 | 7 | 0 | 1 | 88% |
| 向量数据库 | 7 | 0 | 0 | 100% |
| RAG 检索器 | 5 | 0 | 0 | 100% |
| RAG 加载器 | 11 | 0 | 0 | 100% |
| RAG 索引器 | 4 | 0 | 0 | 100% |
| 数据库模型 | 14 | 0 | 0 | 100% |

---

## **1. FastAPI 路由 (backend/main.py)**

| **端点** | **方法** | **状态** | **备注** |
| --- | --- | --- | --- |
| `/health` | GET | ✅ | 系统健康检查（含 DB + 向量库状态） |
| `/auth/register` | POST | ✅ | 用户注册（含密码哈希） |
| `/auth/login` | POST | ✅ | JWT 生成已实现 |
| `/profile` | GET | ✅ | 调用 profile_svc.get_profile() |
| `/profile` | PUT | ✅ | 调用 profile_svc.create_or_update_profile() |
| `/profile/history` | GET | ✅ | 调用 profile_svc.get_profile_history() |
| `/chat/{session_id}` | POST | ✅ | 支持 SSE 流式和普通模式；生成资源自动持久化到 resource_meta |
| `/chat/sessions` | GET | ✅ | 查询 chat_session 表（路由顺序已修复，置于 `{session_id}` 之前） |
| `/chat/sessions` | POST | ✅ | 创建对话会话 |
| `/kg/graph` | GET | ✅ | 构建 kg_node + kg_edge 子图 |
| `/generate` | POST | ✅ | 创建任务 + 后台调用 run_generation |
| `/generate/{task_id}/status` | GET | ✅ | 调用 resource_svc.get_task_status() |
| `/resources` | GET | ✅ | 调用 resource_svc.list_resources() |
| `/resources/stats` | GET | ✅ | 按类型统计资源数量 |
| `/resources/{resource_id}` | GET | ✅ | 调用 resource_svc.get_resource() |
| `/resources/{resource_id}` | DELETE | ✅ | 调用 resource_svc.delete_resource() |
| `/resources/{resource_id}/quiz` | GET | ✅ | 查询 quiz_item 表 |
| `/quiz/submit` | POST | ✅ | 评分并写入 quiz_attempt |
| `/quiz/attempts` | GET | ✅ | 新增：分页查询测验记录 |
| `/pathways` | GET | ✅ | 新增：调用 pathway_svc.list_pathways() |
| `/pathways` | POST | ✅ | 新增：调用 pathway_svc.create_pathway() |
| `/pathways/{path_id}` | GET | ✅ | 新增：调用 pathway_svc.get_pathway() |
| `/pathways/{path_id}` | PUT | ✅ | 新增：调用 pathway_svc.update_pathway() |
| `/pathways/{path_id}` | DELETE | ✅ | 新增：调用 pathway_svc.delete_pathway() |
| `/pathways/{path_id}/items` | POST | ✅ | 新增：调用 pathway_svc.add_pathway_item() |
| `/pathways/{path_id}/items/{item_id}` | PUT | ✅ | 新增：更新路径条目 |
| `/pathways/{path_id}/items/{item_id}` | DELETE | ✅ | 新增：删除路径条目 |
| `/records` | POST | ✅ | 调用 resource_svc.record_learning() |
| `/records` | GET | ✅ | 新增：调用 resource_svc.list_learning_records() |

---

## **2. Agent 节点 (backend/agents/)**

| **文件** | **函数** | **状态** | **备注** |
| --- | --- | --- | --- |
| graph.py | `build_graph()` | ✅ | LangGraph 拓扑完整 |
| graph.py | `get_graph()` | ✅ | 返回编译后图实例 |
| graph.py | `invoke()` | ✅ | 执行完整图；启动时加载已有画像 |
| graph.py | `stream_invoke()` | ✅ | 流式执行图事件；启动时加载已有画像 |
| graph.py | `_run_with_db()` | ✅ | 新增：DB session 包装器 |
| planner_agent.py | `run()` | ✅ | LLM 意图分析 + 知识点匹配；支持知识图谱为空时 fallback |
| planner_agent.py | `route_by_resource_type()` | ✅ | 条件路由逻辑完整 |
| profile_agent.py | `run()` | ✅ | LLM 画像提取 + DB 合并 + 完整性检查；使用 RunnableConfig 接收 db |
| profile_agent.py | `route_after_profile()` | ✅ | 路由到 planner 或 END |
| doc_agent.py | `run()` | ✅ | RAG 检索 + LLM 生成 Markdown 文档 |
| mindmap_agent.py | `run()` | ✅ | RAG 检索 + LLM 生成 ECharts JSON |
| quiz_agent.py | `run()` | ✅ | 按画像难度分配题型 + LLM 生成题目 |
| quiz_agent.py | `save_quiz_items()` | ✅ | 批量写入 quiz_item 表 |
| code_agent.py | `run()` | ✅ | RAG 检索 + LLM 生成代码示例 |
| summary_agent.py | `run()` | ✅ | RAG 检索 + LLM 生成复习摘要 |
| safety_agent.py | `run()` | ✅ | LLM 内容质量审核 + 幻觉检测 |
| safety_agent.py | `should_skip_safety()` | ✅ | 检查 draft_content 是否存在 |
| recommend_agent.py | `run()` | ✅ | KG 查询 + LLM 推荐；不覆盖已有 final_content |

---

## **3. Profile 服务 (backend/services/profile.py)**

| **函数** | **状态** | **备注** |
| --- | --- | --- |
| `get_profile()` | ✅ | 查询 student_profile 表 |
| `create_or_update_profile()` | ✅ | upsert + 写入 profile_history |
| `get_profile_history()` | ✅ | 查询 profile_history 表 |
| `merge_chat_updates()` | ✅ | 增量合并对话更新到画像 |
| `build_profile_context()` | ✅ | 序列化画像为 prompt 上下文字符串 |

---

## **4. Resource 服务 (backend/services/resource.py)**

| **函数** | **状态** | **备注** |
| --- | --- | --- |
| `get_resource()` | ✅ | 查询 resource_meta 表 |
| `list_resources()` | ✅ | 分页查询（含过滤） |
| `delete_resource()` | ✅ | 物理删除资源 |
| `create_generation_task()` | ✅ | 写入 generation_task + resource_meta |
| `get_task_status()` | ✅ | 查询 generation_task 表 |
| `update_task_progress()` | ✅ | 更新任务进度/状态 |
| `record_learning()` | ✅ | 写入 learning_record |
| `list_learning_records()` | ✅ | 查询 learning_record 表 |

---

## **4.5 Pathway 服务 (backend/services/pathway.py) — 新增模块**

| **函数** | **状态** | **备注** |
| --- | --- | --- |
| `get_pathway()` | ✅ | 查询学习路径（含关联条目） |
| `list_pathways()` | ✅ | 列出用户所有学习路径 |
| `create_pathway()` | ✅ | 创建学习路径 |
| `update_pathway()` | ✅ | 更新路径标题/描述（含权限校验） |
| `delete_pathway()` | ✅ | 删除路径（级联删除条目） |
| `add_pathway_item()` | ✅ | 添加知识点到路径 |
| `update_pathway_item()` | ✅ | 更新条目排序/完成状态 |
| `remove_pathway_item()` | ✅ | 移除路径条目 |
| `_item_to_out()` | ✅ | 辅助：ORM → 输出 schema |
| `_path_to_out()` | ✅ | 辅助：ORM → 输出 schema（含排序） |

---

## **4.6 Generation 服务 (backend/services/generation.py) — 新增模块**

| **函数** | **状态** | **备注** |
| --- | --- | --- |
| `run_generation()` | ✅ | 后台任务编排：调用 LangGraph + 持久化结果 |
| `_persist_content()` | ✅ | 持久化非 quiz 资源（mindmap JSON / 文本） |
| `_persist_quiz()` | ✅ | 持久化测验题目到 quiz_item 表 |

---

## **5. LLM 服务 (backend/services/llm.py)**

| **函数** | **状态** | **备注** |
| --- | --- | --- |
| `chat_completion()` | ✅ | 含重试 + Qwen 配额自动切换 |
| `stream_chat_completion()` | ✅ | 流式版本，含配额处理 |
| `get_embedding()` | ✅ | 路由到本地 BGE-M3 或 Spark API |
| `_local_embedding()` | ✅ | 使用 sentence-transformers BGE-M3 |
| `_spark_embedding()` | ❌ | 需调用 Spark Embedding API |
| `_make_client()` | ✅ | OpenAI 兼容客户端工厂 |
| `_is_quota_error()` | ✅ | 检测配额耗尽 |
| `_get_embedding_model()` | ✅ | 新增：BGE-M3 单例加载器 |

---

## **6. 向量数据库 (backend/db/vector.py)**

| **函数** | **状态** | **备注** |
| --- | --- | --- |
| `init_vector_db()` | ✅ | 初始化 ChromaDB 持久化客户端 |
| `get_collection()` | ✅ | 返回默认集合 |
| `get_or_create_collection()` | ✅ | 获取或创建命名集合 |
| `upsert_documents()` | ✅ | 批量 upsert 含 embedding |
| `query_documents()` | ✅ | 向量相似度搜索 |
| `delete_documents()` | ✅ | 按 ID 删除 |
| `health_check()` | ✅ | 检查向量库可用性 |

---

## **7. RAG 管道**

### **加载器 (backend/rag/loader.py)**

| **函数** | **状态** | **备注** |
| --- | --- | --- |
| `load_file()` | ✅ | 文件格式分发器 |
| `load_directory()` | ✅ | 递归目录扫描 |
| `split_text()` | ✅ | 含 overlap 的文本分块 |
| `_load_pdf()` | ✅ | PyPDFLoader 解析 |
| `_load_docx()` | ✅ | UnstructuredWordDocumentLoader |
| `_load_markdown()` | ✅ | 按标题章节分割 |
| `_load_txt()` | ✅ | TextLoader 加载 |
| `_split_markdown_by_headers()` | ✅ | 新增：正则标题解析 |
| `docs_to_chunks()` | ✅ | 新增：LangChain Document → TextChunk |
| `load_file_as_documents()` | ✅ | 新增：返回 LangChain Documents |
| `load_directory_as_documents()` | ✅ | 新增：批量 Document 加载 |
| `TextChunk` | ✅ | 文本块数据结构 |

### **检索器 (backend/rag/retriever.py)**

| **函数** | **状态** | **备注** |
| --- | --- | --- |
| `retrieve()` | ✅ | 语义搜索含阈值过滤；知识库为空时优雅降级返回空列表 |
| `retrieve_by_kp()` | ✅ | 知识点专项检索 |
| `format_context()` | ✅ | 格式化结果含来源引用 |
| `_parse_results()` | ✅ | 转换 ChromaDB 结果 |
| `RetrievedChunk` | ✅ | 检索块数据结构 |

### **索引器 (backend/rag/indexer.py)**

| **函数** | **状态** | **备注** |
| --- | --- | --- |
| `index_chunks()` | ✅ | 批量 embedding + 向量库 upsert |
| `index_file()` | ✅ | 加载文件并索引 |
| `index_directory()` | ✅ | 递归目录索引 |
| `_embed_batch()` | ✅ | 并发 embedding（含信号量） |

---

## **8. 数据库模型 (backend/db/models.py)**

| **表** | **状态** | **备注** |
| --- | --- | --- |
| `user` | ✅ | 用户账户 |
| `student_profile` | ✅ | 学生画像 |
| `profile_history` | ✅ | 画像版本历史 |
| `chat_session` | ✅ | 对话会话 |
| `chat_message` | ✅ | 对话消息 |
| `kg_node` | ✅ | 知识图谱节点 |
| `kg_edge` | ✅ | 知识图谱边 |
| `resource_meta` | ✅ | 学习资源元数据（kp_id 外键已移除，改为自由文本） |
| `generation_task` | ✅ | 异步生成任务追踪 |
| `quiz_item` | ✅ | 测验题目 |
| `quiz_attempt` | ✅ | 测验提交记录 |
| `learning_path` | ✅ | 个性化学习路径 |
| `learning_path_item` | ✅ | 路径条目含进度 |
| `learning_record` | ✅ | 学习活动日志 |

---

## **⚠️ 已知问题**

1. **`_spark_embedding()` 未实现** — 仍为 `raise NotImplementedError`，但有本地 BGE-M3 兜底
2. **`add_pathway_item()` 中 kp_name 为占位值** — 直接使用 kp_id 作为 kp_name
3. **知识图谱表（KGNode/KGEdge）为空** — planner 和 recommend agent 已做 fallback，但精准度受限
4. **部分子页面缺少 `init_session_state()` 调用** — profile、generate、pathway、evaluate 页面可能在直接访问时报错
5. **Agent 调试日志未清理** — profile_agent、planner_agent 中有 WARNING 级别调试日志

---

## **🎯 已解决的关键缺口（对比 2026-04-20）**

| **原缺口** | **当前状态** |
| --- | --- |
| 所有数据库 CRUD 操作 (profile, resource 服务) - 0% | ✅ 100% 完成，新增 pathway、generation 服务 |
| 所有 Agent 节点逻辑 - 31% | ✅ 100% 完成，全部 9 个 Agent 已实现 |
| 文档加载器 (PDF, DOCX, Markdown, TXT) - 50% | ✅ 100% 完成，新增 LangChain Document 适配 |
| 大部分依赖服务的 FastAPI 端点 - 28% | ✅ 100% 完成，路由从 18 个扩展到 29 个 |
| `/generate` 后台任务未接入 Agent | ✅ generation.py 已实现完整编排 |
| `/kg/graph` 未实现 | ✅ 已实现 |

### **架构设计层面（仍存在）**

1. **~~前端无聊天页面~~** → ✅ 已解决：`6_chat.py` 实现对话式画像构建 + 资源生成
2. **学习路径更新机制断裂** — `recommend_agent` 输出推荐结果存入 `state.metadata["recommendations"]`，但没有逻辑将其持久化写入 `learning_path` / `learning_path_item` 表
3. **知识图谱是静态预导入数据** — 需通过 `knowledge_points.json` + `dependencies.json` 预先导入，无动态生成机制
4. **~~画像构建方式与设计文档不符~~** → ✅ 已解决：`6_chat.py` 实现对话式画像构建，profile_agent 从对话中提取画像并持久化到 DB

---

## **📝 更新日志**

| **日期** | **更新内容** |
| --- | --- |
| 2026-04-20 | 初始化实现状态表 |
| 2026-04-20 | 完成 `/auth/register` 端点实现 |
| 2026-04-20 | 补充架构设计层面缺口（对话分析：agent 协作、前端交互、功能点映射） |
| 2026-04-25 | 全面更新：后端实现从 66% → 99%。Profile/Resource/Pathway/Generation 服务全部完成；9 个 Agent 全部实现；RAG 加载器补全；路由从 18 → 29 个。仅剩 `_spark_embedding()` 未实现 |
| 2026-04-26 | 端到端联调：修复 20+ 个 bug 实现完整对话式资源生成流程。主要修复：数据库配置路径、SQLite 兼容、.env 加载、路由冲突、LangGraph config 传递（RunnableConfig）、画像持久化、planner 知识图谱为空 fallback、recommend_agent 不覆盖已生成内容、RAG 知识库为空优雅降级、资源自动持久化到 resource_meta 表。前端聊天页面（6_chat.py）已可正常使用。LLM 切换至通义千问（qwen-plus）。详见 `docs/2026-04-26_调试修复记录.md` |